import { DownloadOutline, PencilOutline, RefreshOutline, SwapHorizontalOutline } from "@vicons/ionicons5";

export const icons = {
  edit: PencilOutline,
  refresh: RefreshOutline,
  download: DownloadOutline,
  switch: SwapHorizontalOutline,
}
